#include <stdio.h>

int main() {
	int n;
	printf("n = ");
	scanf("%d", &n);

	double pi = 0.;
	/* TODO: Add the code below to compute value of pi using the formula given in the pdf */






	printf("PI = %.10f\n", pi);
	return 0;
}
